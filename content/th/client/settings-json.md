# Settings.json

`Settings.json` เก็บค่าฝั่ง client เช่น audio devices, saved servers และ network preferences

โดยปกติให้แก้ผ่านหน้า Settings ก่อน แก้ไฟล์ JSON โดยตรงเฉพาะเมื่อจำเป็นหรือกำลัง debug

หลังแก้ไฟล์โดยตรง ให้ปิดและเปิด client ใหม่เพื่อให้ค่าถูกโหลดใหม่
