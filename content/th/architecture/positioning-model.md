# Positioning Model

VoiceCraft รองรับ positioning จากฝั่ง server/integration หรือจาก client ตามรูปแบบ deployment

สำหรับ BDS และ plugin ส่วนใหญ่ integration จะส่งตำแหน่ง authoritative เข้า server

ตรวจ `VoiceCraftConfig.PositioningType` เมื่อ proximity ไม่ตรงกับตำแหน่งในเกม
