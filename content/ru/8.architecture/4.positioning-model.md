# Positioning model

VoiceCraft поддерживает и server-side, и client-side positioning model.

## `PositioningType`

- `0 = Server`
- `1 = Client`

Это значение должно совпадать между сервером и клиентом.
