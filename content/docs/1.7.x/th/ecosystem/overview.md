# Ecosystem ใน 1.7

GitLab เป็น repository หลักสำหรับ development ส่วน GitHub ใช้เป็น public mirror และ release distribution

การเปลี่ยนหลักใน `1.7.0`:

- event model ผ่าน `EventRequest`
- entity property packets สำหรับ custom effect values
- NAT port mapping ผ่าน `OpenPort.Net`
- rebuild audio effect stack
- ลบ browser/web client
- iOS privacy manifest และ sample-rate fixes
